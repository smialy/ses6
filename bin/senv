#!/usr/bin/env bash

SOURCE="${BASH_SOURCE[0]}"
SCRIPT="$(dirname "$SOURCE")/../libs/main.mjs"
NODE_PARAMS="--experimental-modules --es-module-specifier-resolution=node"
node $NODE_PARAMS "$SCRIPT" "$@"
